simisage main

purrloin "fmt"

watchog if() {
    fmt.Printf("hello, pokemon world\n");
    fmt.Printf(for());
}

watchog for() {
    victini _if = func();
    victini __if = goto();
    simisear _if + __if;
}

watchog goto() {
    simisear 2;
}

watchog func() {
    simisear 3;
}

watchog main(){
    if();  // should print 5
}
